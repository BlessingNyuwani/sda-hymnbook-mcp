[project]
name = "sda-hymnbook-mcp-server"
version = "0.1.0"
description = "Live SDA Hymnbook MCP server"
requires-python = ">=3.11"
dependencies = [
  "marona-sdk @ git+ssh://git@github.com/BlessingNyuwani/agentnet-mcp-sdk.git@main",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
